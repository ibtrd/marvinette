import './Button.css'

export default function Button({isOpen, ...props}) {

	return (
		<div>
			<div>
				<h3>Wheel may have changed...</h3>
				<p>Please reload the page.</p>
				
			</div>
		</div>
	);
  }

